sudo: false
language: node_js
node_js:
  - stable
cache: yarn
script:
  - commitlint-travis
  - yarn test
git:
  depth: 5
after_success:
  - npm run build
  - npm run semantic-release
branches:
  only:
    - master
